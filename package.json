{
  "name": "my-blog-ai",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "sync:files": "node scripts/sync-files.mjs",
    "sync:files:force": "node scripts/sync-files.mjs --force",
    "prebuild": "node scripts/sync-files.mjs",
    "predev": "node scripts/sync-files.mjs"
  },
  "dependencies": {
    "astro": "^7.1.0"
  }
}